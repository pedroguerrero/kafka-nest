## Kafka integration on NestJs

## Run the app

```bash
$ cd kafka-compose
$ docker-compose up -d
```

## View the app logs

```bash
$ docker-compose logs -f app
```

To send a message go to [here](http://localhost:9021)
